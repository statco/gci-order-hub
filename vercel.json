{
  "outputDirectory": ".",
  "functions": {
    "api/order-router":    { "maxDuration": 30  },
    "api/authorize-order": { "maxDuration": 30  },
    "api/walmart-sync":    { "maxDuration": 300 },
    "api/walmart-item-feed":   { "maxDuration": 300 },
    "api/walmart-feed-status": { "maxDuration": 10 }
  },
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ],
  "crons": [
    {
      "path": "/api/walmart-sync",
      "schedule": "0 9 * * *"
    }
  ],
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" }
      ]
    }
  ]
}
